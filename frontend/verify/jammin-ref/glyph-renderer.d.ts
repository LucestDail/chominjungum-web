/** jammin 원본 렌더러(복사본)의 최소 타입 선언 — 대조용. */
export function buildHangulWrapperHtml(opts: {
  jsonArray: unknown[];
  fullWord: string;
  ordering: number | string;
  profileKey: 'editor' | 'compact';
  toolbar: string;
  rowWrapperStyle?: string;
}): string;
